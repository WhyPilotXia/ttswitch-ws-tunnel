# 内网模型接入 harness 说明

## 结论

`腾讯模型访问demo.py` 里的模型通道（TT Switch 公网隧道 → 内网模型网关）已完整接入本机
DeepSeek Harness：

- **Provider 路由**：`tt-switch`（显示名「内网模型 TT Switch」）
- **网关**：`http://118.31.105.6:18443/tencent/v1`（OpenAI 兼容协议，Bearer 认证）
- **凭据**：`TT_SWITCH_API_KEY`（已存 harness 凭据库，write-only，文件 `~/.dsh/.credentials.yaml`）
- **模型**：网关全部 31 个模型已注册（GLM/Claude/GPT/Gemini/Kimi/MiniMax/Hunyuan/DeepSeek 各系）
- **默认模型**：demo 的 `glm-5.3-ioa` 已包含在目录中（当前 harness 全局默认仍是 deepseek-official，未改动）

## 关于「PID 值」

demo 文件里并没有字面意义上的 PID。我将其理解为**把这条模型通道的接入信息
（服务器地址 + token + 模型）整体接入 harness**——这些已全部落地：

| demo.py 中的值 | harness 中的落点 |
|---|---|
| `SERVER_URL = http://118.31.105.6:18443` | `baseURL` 前缀（+ `/tencent/v1`） |
| `TT_TOKEN = ttsw-...` | 凭据 `TT_SWITCH_API_KEY`（apiKeyEnv 引用，settings.yaml 不含密钥；令牌不写入代码，经环境变量 `TT_TOKEN` / `.env.txt` 注入） |
| `DEFAULT_MODEL = glm-5.3-ioa` | 模型目录条目 `glm-5.3-ioa` |

如果你说的 PID 是别的含义，把名字告诉我，我可以对应调整。

## 配置位置（全部热生效，无需重启）

1. `~/.dsh/settings.yaml` → `llm-pi-ai.providers.tt-switch`（Provider 定义，见 `tt-switch-provider.yml`）
2. `~/.dsh/.credentials.yaml` → `refs.TT_SWITCH_API_KEY`（密钥，经 `credentials.set` 写入）

两份文件都有 chokidar 监听：外部编辑即时发布，下一次请求即生效。

## 使用方式

- **Web UI 模型页**（设置 → 模型）：出现「内网模型 TT Switch」卡片，可查看/切换 31 个模型，
  每个模型可选手动推理等级（Off / Minimal / Low / Medium / High）。
- **费用标注**：模型名称后缀已注明费用类型——
  `-ioa` 后缀 = 免费，名称标「**内网·免费**」（共 12 个：GLM/Kimi/MiniMax/Hunyuan/DeepSeek 各系）（后来确认）；
  无后缀 = 公费，名称标「**公费**」（共 19 个：Claude/GPT/Gemini 全系）。选模型时按名称备注区分即可。
- **切换 agent 全局默认模型**：改 `settings.yaml` 的 `agent-default-model`，例如：
  ```yaml
  agent-default-model:
    provider: tt-switch
    model: glm-5.3-ioa
    reasoningEffort: high
  ```
  或直接在 Web UI 的默认模型选择器里选。

## 稳定性与性能参数（实测对齐）

- **协议实测**：网关为标准 OpenAI chat.completions——`system`/`developer` 角色、
  `max_tokens`/`max_completion_tokens`、`reasoning_effort`（none/minimal/low/medium/high）、
  SSE 流式 `reasoning_content` 全部 200 实测通过，pi-ai 的 `openai-completions` 协议原生匹配。
- **`streamIdleTimeoutMs: 600000`**：流空闲 10 分钟（对齐 demo 的 600s），不设总超时，
  长推理/长生成不会被中途掐断。
- **`reasoning: high`**：部署默认推理等级高，发挥模型最大性能；可按请求下调。
- **`retryPolicy`**：normal 模式、最多 3 次、500ms→10s 抖动退避，隧道抖动自动重试。
- **容量**：路由默认 context 262144 / maxTokens 32768（网关实测接受 32768 输出上限）；
  Claude `-1m` 变体声明 100 万上下文。
- **凭据解析**：`apiKeyEnv` 引用 → harness 凭据服务（file 源，热重载），settings.yaml 永不落密钥。

## 分享给其他人（最快方式）

把 `分享-内网模型TT-Switch.ps1` 发给对方，对方只需两步：

1. 确认自己的 DeepSeek Harness Web 已在本机启动（默认 `http://127.0.0.1:3080`）
2. 运行（Windows PowerShell 5.1 或 PowerShell 7 均可）：
   ```powershell
   powershell -ExecutionPolicy Bypass -File 分享-内网模型TT-Switch.ps1
   ```
   或
   ```powershell
   pwsh -File 分享-内网模型TT-Switch.ps1
   ```

脚本走 harness 自带 RPC（`credentials.set` + `settings.mutate`），由服务端完成 YAML 合并、
schema 校验、原子写盘和热发布——**不直接改对方的 settings.yaml 原始文本**，因此不会因为缩进/
引号/重复键把配置搞坏，也**只新增/覆盖 `tt-switch` 这一条 provider，不动其他任何配置**。

可选参数：

| 参数 | 默认值 | 用途 |
|---|---|---|
| `-ApiKey` | 环境变量 `TT_TOKEN`（令牌不再内置） | 换成对方自己的 TT Switch token，或先 `$env:TT_TOKEN = "ttsw-..."` |
| `-Port` | 3080 | 对方 Web GUI 不是 3080 时指定 |
| `-BaseURL` | `http://118.31.105.6:18443/tencent/v1` | 网关地址变化时指定 |

示例（对方独立 token、非默认端口）：
```powershell
pwsh -File 分享-内网模型TT-Switch.ps1 -Port 3081 -ApiKey "ttsw-对方的token"
```

脚本是**幂等**的：重复运行结果一致，报错会明确提示（连不上 harness / RPC 失败），
不会留下半截配置。运行成功后，对方 Web「设置 → 模型」页立即出现「内网模型 TT Switch」。

## 常见问题

- **401 / MISSING_CREDENTIAL**：token 失效或未写入。检查 `credentials.describe` 中
  `TT_SWITCH_API_KEY` 是否 configured=true；重新在 Web 模型页粘贴密钥即可（write-only 存储）。
- **网关不通**：公网隧道依赖内网 Agent 在线（`GET {origin}/__tunnel_health` 可查）。
  短暂中断由重试策略兜底；长时间中断需检查内网侧 Agent。
- **某个模型报错**：网关为统一 OpenAI 兼容面，31 个模型均已实测 `reasoning_effort`/`max_tokens`；
  若个别模型行为异常，可在 settings.yaml 中单独调整该条目或删除。
- **改完没生效**：settings.yaml 由 watcher 热发布；确认文件是合法 YAML（非法内容会保留上一份好配置并告警）。
