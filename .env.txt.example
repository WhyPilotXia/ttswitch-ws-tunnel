# 复制为 .env.txt 并填入真实值；.env.txt 已被 .gitignore 排除，不会提交到仓库。

# ── 内网服务器（intranet_agent.py）读取 ──
# 本机 Mac（TT Switch 所在）的内网 IP；DHCP 变化后只改这一行。
# Mac 上执行 ifconfig en7 核对有线网卡地址。
IP=10.x.x.x

# ── 外网客户端（腾讯模型访问demo.py）读取 ──
# TT Switch 访问令牌（ttsw-...）；优先级：环境变量 TT_TOKEN > 本文件。
TT_TOKEN=ttsw-<你的token>
